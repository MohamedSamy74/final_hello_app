
apiVersion: v1
kind: Service
metadata:
  labels:
    app: myservice
  name: myservice
spec:
  ports:
  - name: final_project_port
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: final_project
  type: NodePort
status:
  loadBalancer: {}
